# Tasks

## to do

- Ship accounts and answers to prod, in this order: `npm run deploy` (schema, auth username component, agent component, static site), then `npx convex run --prod migrations:backfillVisibility '{}'`, `npx convex run --prod migrations:backfillUsers '{}'`, and `npx convex run --prod migrations:backfillDaily '{}'` (fills `userDaily` for the profile charts), then confirm the AI Gateway is enabled on the prod deployment (a signed in ask should reach `answerStatus: "done"`). PRD `prds/ask-anything-accounts.md`.
- Sign in as the admin on dev `/admin` and click through the Users tab: pause the throwaway account, confirm the paused banner and disabled composer, block it, confirm its public asks blur and the email lands in `blockedEmails`, restore it. The agent could not do this pass without the admin password.
- Upload a profile photo on `/me` once and confirm the avatar shows in the top row, on wall cards, and on `/:handle`.
- Throwaway account `profiledemo` (dev) was left in place so the profile page has data to show. Delete it from `/me` or the admin Users tab when done looking.
- Switch Jev to the Convex AI Gateway when Convex lists it. The prompt is in `prds/ask-anything-accounts.md` under "Switch Jev to the Convex AI Gateway".
- Auth v2 alpha has no `deleteUser` on the core component, so `deleteAccount` leaves one inert `accounts` row per deleted user. Revisit when the component ships one.
- Separately authorize a dev deployment and run the full OAuth browser round-trip checks. Separate dev and production clients, credentials, and allowed origins are configured and privately verified. Google consent is External/In production. OAuth code is not deployed; dev and production code deployment remain prohibited. See `prds/google-github-sign-in.md`.
- Post a few mean or edgy messages and tune `BLOCK_THRESHOLD` in `convex/questions.ts` against what the answers toggle shows. Long fifteen word asks are a good stress test.
- Verify the failed and retry path once by temporarily breaking network or the key.
- Redeploy if `/admin` on prod still fails: `npm run deploy`.
- Create the prod admin account, in this order: `npm run deploy` (ships the sign up window), `npx convex env set ADMIN_SIGNUP_OPEN 1 --prod`, visit `/admin` on prod and create the account with the `ADMIN_USERNAME` email, then `npx convex env remove ADMIN_SIGNUP_OPEN --prod`. Confirm the create account button is gone after. PRD `prds/sec-check.md`.
- Confirm the single `users` row on dev and prod is yours: `npx convex data users` and `npx convex data users --prod`.
- Prod hold is off until `HELD_TERMS` is set there; `npm run deploy` never copies env vars. Order matters: deploy first (the new whole word matcher), then `npx convex env set HELD_TERMS "$(npx convex env get HELD_TERMS)" --prod`. Setting it before the deploy would run the list through the old substring matcher and hold "merry christmas" on `christ`.
- After the next prod deploy, backfill the two misread prod rows: `npx convex run --prod messages:overrideReply '{"messageId":"j5731bkb3n22m7m188nvvzeqgn8ej3n8","reply":"yes"}'` and the same for `j5798c6x9x69t3kef7kndbm5z18ejkja`.
- Either or pick: speculative `pick` Choice in the same judge request when an ask contains " or ". Plan and prod numbers in `prds/ask-shape.md`. Build once "or" asks are more than a couple percent of the wall.
- Decide whether "How does it feel?" stays. Nearly every question scores flat. Options in `prds/ask-shape.md` section 3. The mood row is gone from the card fold as of 2026-09-20; the chip in the card foot and the wall mood line still use it.
- Ship the answers fold, votes, and copy link to prod: `npm run deploy` carries the schema (`votes` table, four optional message fields). No backfill; old rows have no runner up and zero votes. Then watch `stats.agreement` for a week against `replyConfidence` before touching `BLOCK_THRESHOLD` or `CLOSE_CALL`. PRD `prds/how-sure-and-votes.md`.
- Test the vote rate limit by hand once: eleven taps in a few seconds on one card should surface "Slow down, Ns" under the thumbs. The agent verified new, flip, and take back but not the burst.
- Ship follow up threads to prod: `npm run deploy` carries the schema (`threads` table, optional `followUps` on `messages`). No backfill: existing owner threads with real follow ups will not show "Read the thread" until the asker sends one more, since `followUps` starts at zero. If that matters, write a one off migration that counts agent messages per `threadId` minus two. PRD `prds/follow-up-threads.md`.
- OAuth provider setup details remain in ignored `setup-off-Google-GitHub.md`. Google requires the exact Convex hostname as an authorized domain; bare `convex.site` is rejected. Google consent is External/In production. All live OAuth checks remain pending; dev and production credential configuration is complete.
- Side thread on a held or hidden ask: `laneFor` returns null so the composer never renders, but a thread opened before the hide stays listed under `/me` Follow ups with a `closed` tag. Decide whether hiding an ask should also hide those threads from their owners.

## completed

- 2026-09-21 02:55 UTC Brand mark and logo. `public/icon.svg` and `icon.png` (1024): ember card, Archivo ExtraCondensed Black J as a filled path, sulfur period, centered for a circle crop. `public/logo.svg` and `logo.png` (3000 wide, transparent): mark plus "Ask Jev" wordmark in the same face. Outlines came from the Archivo variable font (fonttools instancer at wdth 62 wght 900, fontkit for paths) so no font is needed at render time. Previewed at 64 px, in a circle mask, and on limestone. Favicon, ico, and apple touch icon untouched. No deployment.

- 2026-09-20 13:02 UTC Configured separate production Google/GitHub clients after explicit confirmation, saved and privately verified all four credentials and allowed origins in Convex production, and verified saved provider callbacks. Published Google consent and confirmed External/In production. The user completed GitHub verification with Google Authenticator. No code deployment, credential files, commit, or push.

- 2026-09-20 12:53 UTC Diagnosed Google failure on both requested localhost ports: the dev backend lacks `auth:startSignInGoogle`; GitHub also lacks its start function. Both return origins and all dev credentials are already configured. Read-only metadata confirms OAuth functions are absent on dev and prod. Prepared production credential forms; their creation/storage and Google consent publication await confirmation. No deployment or code changes.

- 2026-09-20 05:03 UTC Saved and privately verified the remaining GitHub dev client secret after the user completed Google Authenticator verification. Both provider credential pairs and the origin allowlist are now present in Convex dev. No deployment, local credential file, commit, or push.

- 2026-09-20 04:59 UTC Verified both user-created dev OAuth clients. Saved Google client ID/secret and GitHub client ID only in Convex dev, then compared the saved values privately without printing them. GitHub requested fresh identity verification before secret generation. No deployment or local credential file.

- 2026-09-20 04:30 UTC Implemented local Google/GitHub OAuth with verified-email linking, admin and moderation guards on first and returning sign in, provider metadata, same-origin return paths, global errors, provider buttons, password-only admin, and header controls. Passed 23 tests, both typechecks, build, and mobile light/dark UI checks. Created Google project/branding with approved policy acceptance and set dev localhost origins. No deployment, static upload, commit, or push. Credential creation remains pending confirmation. PRD `prds/google-github-sign-in.md`.

- 2026-09-20 03:45 UTC Wrote `setup-off-Google-GitHub.md`, the Computer Use handoff for Google and GitHub sign in on dev and prod with Convex Auth v2 alpha, and added it to `.gitignore`. Written from the installed package's types (`node_modules/@convex-dev/auth/dist/oauth/`), since the v2 docs site does not list OAuth yet. Names the deployments and the console accounts, so it stays out of git; the admin is referred to only as `ADMIN_USERNAME`. No code changed.
- 2026-09-20 02:00 UTC Follow up threads on every ask. PRD `prds/follow-up-threads.md`. Schema: new `threads` table (`by_message_and_user`, `by_message`, `by_threadId`, `by_user`), `messages.followUps`. `convex/answer.ts`: `laneFor`, `acceptsFollowUps`, `sideThreadFor`, `sideSystem`, `readAccess`; `followUp` branches into own or side and opens the side thread with `createThread` on the first send; `reply` takes `system`; `listMessages` gates through `readAccess`; `myThreads`; `deleteSideThreadsOn` and `deleteSideThreadsFor`. `messages.get` returns the viewer's `followUp` lane and `hasThread` now needs `followUps > 0`. `profile.remove` and `deleteAccount` clean side threads. New `src/components/FollowUp.tsx`; wired into Wall, Yours, Profile, Me. `AnswerBlock` loses Open thread. `AskPage` renders `Threads` with two lanes, a visitor sign in card, and `#follow-up` focus. `SignIn` honors `?next=`. `/me` gains Follow ups with a fourth anchor. `questions.modelLabel`. Docs (answers, account page, data sections), Privacy (side thread storage and visibility), changelog, files updated. Verified on dev in the browser: visitor wall cards show "Sign in to ask follow up questions" with `next`; sign in from that link landed on `/a/:id#follow-up` with the composer focused; owner lane shows "Keep going" and a single thread; side lane on another user's ask shows "Your follow ups", the model label, and a focused composer; a side thread send produced a contextual model reply and a row in `threads`; `/me` Follow ups listed it; light and dark themes checked. Typecheck and `convex dev --once` clean.

- 2026-09-20 01:30 UTC How sure was Jev, votes, copy link. PRD `prds/how-sure-and-votes.md`. Schema: `messages` gains `replyRunnerUp`, `replyRunnerUpP`, `agree`, `disagree`; new `votes` table with `by_message_and_voter` and `by_voter`. `judge.run` derives the runner up from the reply Choice probabilities and `record` stores it; `overrideReply` clears it. New `convex/votes.ts` (`cast`, `mine`, `deleteVotesFor`), `vote` rate limit bucket, `voteAgree`/`voteDisagree` counters, `stats.agreement`, `deleteAccount` removes votes. `JevAnswers` rewritten: closed by default with `jev:answers-open`, "How sure was Jev", two rows plus a conditional runner up, exports `VerdictChip`, `EdgeTag`, `verdict`, `wallEdge`. New `CopyLink.tsx` and `Vote.tsx`. Wall, AskPage, Profile, Me, Yours swapped to `VerdictChip`; Wall and AskPage carry `EdgeTag`, `CopyLink`, `VoteButtons`; Profile and Me carry `CopyLink` and `AgreedNote`; CostTracker shows "Agreed with Jev" in More numbers. Docs and Privacy updated. Verified on dev in the browser: chips `Jev: yes · 94%`, `Jev: yes · 24%` + `close call`, `Jev: depends · 63%` + `leaning no`; a fresh depends ask stored runner up `no` at 0.28 and the fold showed all three rows; agree then disagree flipped the `votes` row and the message tallies (0 and 1), `stats.agreement` returned total 1, "Agreed with Jev 0%" appeared in the panel; ask page verdict line and `/me` rows render; dark theme checked. Typecheck and `convex dev --once` clean.

- 2026-09-19 22:45 UTC `/me` reordered to Profile, Account, Your asks, with an anchor row under the heading. PRD `prds/ask-anything-accounts.md`. `src/components/Me.tsx`: `ME_SECTIONS` drives a `me__nav` of hash links; sections carry `id="me-profile"`, `me-account`, `me-asks`; `Settings` and `Danger` render before `History`. `src/styles.css`: `.me__nav` ruled row in the docs contents style, `.me__section` gets `scroll-margin-top`, coarse pointer tap area added. Verified on dev: anchor click lands the section 24px from the top, URL `/me#me-asks`, route unchanged. Typecheck clean.
- 2026-09-19 22:40 UTC Form fields on `/me`, `/sign-in`, and `/sign-up` have a visible border. PRD `prds/ask-anything-accounts.md`. `src/styles.css`: `.auth__input` carries its own 1.5px `--input-border` rule, 48px min height, `--radius-small` (16px light, 6px dark, so a single line field is a field and not a pill), focus color on focus, and the native caret kept while empty. The composer redo had moved the border from the textarea to its wrapper box and these fields had been leaning on that rule. Checked in the browser on `/sign-in` and `/me` in both themes.
- 2026-09-19 22:29 UTC Count panel folds, wall header above the fold. PRD `prds/ask-anything-accounts.md`. `src/components/CostTracker.tsx`: three headline rows stay (Jev spend, To one million, At this pace); Per message, Tokens in, Model answers, Answer spend, Per answer, On the clock, and the clock footnote move into a `panel__more` block behind a "More numbers" / "Fewer numbers" toggle, closed by default, remembered in `localStorage` (`jev:numbers-open`), `aria-expanded` and `aria-controls` wired. `src/styles.css`: `.panel__toggle` ruled like a stat row with the answers caret, `.panel__more`, stat labels `nowrap`, `.hero` loses `min-height: 100vh` (the reason the wall was always a screen down) and drops to 48px bottom padding, `#wall.section` gets 48px top padding, coarse pointer tap area for the toggle, the 800px min height override removed. `src/lib/format.ts`: `roughDuration` past a year returns one decimal under ten years and whole years after, no day remainder. Verified in the browser at 1440 by 900: panel bottom 682px, "The wall." heading 814 to 890, above the fold; toggle opens six rows and the footnote, persists across reload; dark theme fits on one line per row; 375px shows no horizontal overflow. Typecheck, prettier, and `vite build` clean.
- 2026-09-19 22:10 UTC Mobile pass on the whole UI. PRD `prds/ask-anything-accounts.md`. Every route checked at 375px in the browser, signed in and out: home, `/docs`, `/me`, `/:handle`, `/a/:id`, account menu. No horizontal overflow anywhere. Fixes in `src/styles.css`: top row reflows under 800px (nav left, avatar and toggle right, credit on its own line via `display: contents` on `.hero__left`, credit links `nowrap`); docs table headers and code wrap under 640px so the price table fits; heatmap dots 5px with 1px gaps under 640px so a year fits 343px; `(pointer: coarse)` rule grows the mono text links, toggles, back link, timestamps, and `?` hints to a 38 to 40px hit area with no layout shift, small ghost pills to 31px. `Composer.tsx`: the Enter hint is a `composer__keyHint` span hidden under 800px; visitor placeholder shortened to "Ask something Jev can settle" so it stays on one line in an empty box. `App.tsx`: credit span gets `hero__credit`. All fields 16px or more so iOS does not zoom. Typecheck and prettier clean. Not checked: `/admin` on a phone (needs the admin password) and the dark theme at 375px (same layout rules, only colors differ).
- 2026-09-19 21:45 UTC Hero redo, composer as one box, gateway credit, profile verified. PRD `prds/ask-anything-accounts.md`. New `src/components/Lanes.tsx` replaces the copy column: bulleted visitor and sign in lists with every number imported from `words.ts`, `limits.ts`, and `questions.ts`; one list when signed in. Example questions removed. `Composer.tsx` is one box with a header row (label, segmented Wall | Private for accounts or a sign in link for visitors), a textarea that grows to about eight lines then scrolls, and a bottom bar with the mono count and a fixed 40px Ask pill. Headline one size smaller, fold starts higher. Convex AI Gateway named in the top row credit, the Lanes list, a How it works paragraph plus the Answers card, and `/docs`; counts derived from `ROUTES`. Verified: signed in as `profiledemo`, one wall ask and one private ask; `userDaily`, `userUsage`, four `messages` rows, and the rendered `/profiledemo` page agree (asks 4, answers 4, wall 2, private 2, models 2 and 2, tokens 5,461, $0.00093, topics nature 3 tech 1; "On the wall" lists only the two public asks). Both typechecks clean. Files: `src/App.tsx`, `src/components/Lanes.tsx`, `src/components/Composer.tsx`, `src/components/HowItWorks.tsx`, `src/components/Docs.tsx`, `src/styles.css`.
- 2026-09-19 21:30 UTC Profiles at `/:handle` and the docs page. PRD `prds/ask-anything-accounts.md`. Router matches fixed routes first, then `/:handle` or `/u/:handle`; `RESERVED_HANDLES` in `convex/lib/auth.ts` keeps handles off app routes at sign up and on rename. New `userDaily` table (one row per user per UTC day: asks, public and private, verdicts, follow ups, answers, tokens, latency, spend, plus reply, topic, and model records) written by `bumpDaily` inside the same mutations as `bumpUsage`; `profile.stats` reads at most 400 rows and zeroes private counts for anyone but the owner or admin; `migrations.backfillDaily` rebuilt it on dev; `deleteAccount` removes the rows. `Profile.tsx` rebuilt: head with visibility pill the owner clicks to flip, Share and Edit, rail, four stat row with streaks, year heatmap, Jev reply split, ranked models with lane reason, tokens area chart, asks bar chart split Wall and Private for the owner, top topics, wall asks. Hand drawn SVG, both themes, stacks at 720px. `byHandle`, `stats`, and `publicAsks` all show a private profile to its owner. New `/docs` page, thirteen sections with a sticky table of contents, every figure imported from the enforcing constant, no admin features, linked from the hero nav, How it works, and the colophon. Verified on dev in the browser with throwaway `profiledemo`: one wall ask and one private ask produced Asks 2, Answers 2, streak 1d, one full intensity heatmap dot, yes 50% and open 50%, two models ranked, Wall (1) Private (1) on the bar chart, nature and tech topics, and the wall ask listed on the private profile for its owner. Docs rendered 13 sections and 5 tables with the right numbers. Typecheck clean, `convex dev --once` clean.
- 2026-09-19 21:00 UTC Ask Jev anything, for real. Hero reads "Ask Jev anything," with a small mono "for real" tag below right (`display__tag`). Copy column switches on sign in state: visitors get the three to fifteen word rule and a sign in link, accounts get the sixty word rule and the blur note. Signed in asks drop the allowlist and the held terms gate: `parseOpenAsk` flags profanity, `messages.send` accepts a held term or blocklist word and sets `wallHidden` on the row. `toPublic` takes the viewer and computes `masked` (admin hidden, or `wallHidden` and not the author or admin); the wall, search, `/a/:id`, and public profiles blur those rows with a `held words` tag for others and `blurred for others` for the author, while `/me` and Yours show them in full. Visitors are unchanged. Composer: "up to 60 words" label, taller textarea, open question examples, chips only for visitors or when a blocklist word means a blur. Open question nudge in Yours when a visitor's ask comes back with Jev's `open` reply, offering sign in for a model answer; no extra call. `heldTerms` normalizes case and punctuation. `profile.setVisibility` no longer reruns the wall rule; `exportData` is unmasked for the owner; admin rows carry `wallHidden`. Verified on dev in the browser with a throwaway account: an ask with a blocklist word posted to the wall, author saw the full text with the tag, a visitor saw the blur with `held words` and no answer block; a visitor ask "why does bread rise in the oven" came back `open` and Yours showed the nudge. Typecheck clean. PRD `prds/ask-anything-accounts.md`.
- 2026-09-19 20:20 UTC Ask Jev anything: gateway answers, accounts, profiles. PRD `prds/ask-anything-accounts.md`. Convex Auth v2 username and password open to everyone (admin sign up window unchanged), `users` gains handle, display name, bio, links, photo, public flag, sequential `userNumber` from a `sequences` table, and `status` (active, paused, blocked) with `blockedEmails` and `userUsage` tables. Jev answers a seventh `route` Choice that picks one of four AI Gateway models (`ROUTES` in `convex/questions.ts`); `convex/answer.ts` streams a short answer into an `@convex-dev/agent` thread through `convexGateway()`, mirrors text onto the row for wall cards, records tokens and micro USD on sharded counters and the user's usage row, and takes follow ups. `convex/lib/jev.ts` is the one door to Jev with a `JEV_PROVIDER` switch and TypeSafe fallback. `messages.send` takes `visibility`; private asks allow sixty words of anything but profanity; signed in limits are twenty a minute with answer budgets of twelve a minute and two hundred a day. `convex/profile.ts`: me, update, photo upload, byHandle, publicAsks, history, setVisibility, setArchived, remove, exportData, deleteAccount. Admin: Asks and Users tabs, Private filter, author and model on rows, setAnswerHidden, users list, search, detail drawer, setUserStatus. Client: `useRoute` and `Link`, `/sign-in`, `/sign-up`, `/me`, `/u/:handle`, `/a/:id`, account menu, Wall | Private toggle and textarea, `AnswerBlock` on cards, Radix tooltips across the app, both themes. Migrations run on dev. Verified end to end in the browser with a throwaway account: private ask judged in 300ms, routed `explain`, Claude Haiku 4.5 answered in 2.3s, follow up streamed, history and profile worked, archive worked, delete account cascaded and left the admin row intact. Typecheck clean.
- 2026-09-19 19:13 UTC Terms and privacy rewritten ahead of the accounts plan (`.cursor/plans/ask_anything_with_accounts_0d3af35d.plan.md`). PRD `prds/terms-privacy.md`. Terms gain Accounts (13+, one per person, no impersonation, no re signup after a block), Public and private asks (private means not shown, not encrypted, admin can see it), Jev's answers and model answers (third party models via the Convex AI Gateway, budgeted per account), Moderation and enforcement (admin may hide asks and answers, remove content, pause, block, restore, change limits, reset or shut down, at any time and in particular when abuse is believed to be happening; reason and timestamp recorded; decision final), Deleting your account (cascade, public asks detached and kept, orphaned password hash, blocked email kept). Who runs it now names Convex Auth and the AI Gateway and the model providers as non parties. Privacy splits collection into everyone vs account holders (email, Argon2id hash, session token, profile, photo, asks, threads, answers, usage row, status and reason, blocked emails), adds model providers to Where the data goes (ask text, verdict, follow ups; no ids), a "Public, private, and what the admin sees" section, retention for account data and blocked emails, and self service export and delete under rights. Both pages say features may ship after the date and are covered when they do. Typecheck and prettier clean. Verified on dev in the browser: both routes render every new section in light mode, no overflow.
- 2026-09-19 18:58 UTC Terms and privacy pages. PRD `prds/terms-privacy.md`. New `src/components/Legal.tsx` with `Terms` and `Privacy` on one `LegalPage` shell (hero top row with back link and theme toggle, heading, last updated, dotted rule sections, terms, privacy, source footer). `App.tsx` routes `/terms` and `/privacy` beside `/admin`. Colophon gains a third mono line under the TypeSafe disclaimer: Terms, Privacy, Source. Copy is written for this app from the code: no accounts, one anonymous UUID, IP read for the five per minute window only, ask text alone goes to TypeSafe, everything live is public, no self service delete. Names Wayne Sutton as maintainer and states that TypeSafe AI and Convex, Inc. are third party providers, not parties, with no responsibility for the Service. Liability cap, indemnity, waiver of legal action, California law kept. Verified in the browser on dev: both routes render in light and dark, back link returns to `/`, colophon link routes to `/terms` with the right title, no horizontal overflow. Typecheck and prettier clean. Ships to prod with the next `npm run deploy`.
- 2026-09-19 18:40 UTC Security review and two fixes. PRD `prds/sec-check.md`. Source review of all 16 Convex functions plus unauthenticated `curl` probes of the dev and prod function APIs: admin queries and `setHidden` throw `Not signed in` (including on a real dev id), `retry` with a wrong session returns the generic `Message not found`, wall and search return the `publicMessage` projection with no `sessionId`, internal functions are unreachable, `npm audit` clean, no secrets in git history. Findings closed in code: sign up now requires `ADMIN_SIGNUP_OPEN=1` and an empty `users` table (`convex/users.ts`, `convex/lib/admin.ts`), `admin.me` returns `signupOpen` and the sign in form only offers "Create the admin account" while open (`convex/admin.ts`, `src/components/Admin.tsx`), `messages.send` requires a 32 to 64 character session id and `src/lib/session.ts` regenerates anything shorter. README env table gains the new var. Typecheck and prettier clean. Prod steps in to do.
- 2026-09-19 00:35 UTC README written for the public GitHub repo (`waynesutton/ask-jev-ai`). What Jev is, how a message travels, a stack table with links (TypeSafe, Convex, sharded counter, rate limiter, Auth v2, static hosting, React, Vite, Phosphor, obscenity, safe words, fonts, Cloudflare), features, local setup, env vars, scripts, layout, cost, credits. Admin is described as a feature with no route or URL. Email addresses removed from `task.md`; the PRDs held none.
- 2026-09-17 09:47 UTC Admin filter and search, hide is a blur. PRD `prds/admin-search-filter.md`. `by_hidden` index; `admin.list` takes `filter` (all, live, blocked, hidden) and picks an index per filter; `admin.search` over `search_text` across every status with the same filter, 30 hits. `messages.toPublic` keeps judgment fields on hidden rows and masks only `text`; `WallCard` renders the full meta row and answers with a "hidden by admin" note; dashed border removed. `Admin.tsx` tools row with `.seg` segmented control and `.search--static` pill, 200ms debounce, hits replace the list. Verified on dev in the browser: Hidden filter shows 5 pulled rows, search "work" gives 5 under Hidden and 6 under All, wall cards show blurred text with chart and chips intact. Typecheck and prettier clean. Ships to prod with the next `npm run deploy` (schema index goes with it).
- 2026-09-17 09:30 UTC Favicon. `public/favicon.svg` (ember square, obsidian compressed J as a filled path), `favicon.ico` with 48, 32, 16 frames, and `apple-touch-icon.png` at 180, all from the SVG via ImageMagick. Three `<link>` tags in `index.html`. Verified the three files serve from Vite with correct content types and the 16 px frame reads as a J. Note: ImageMagick's built in SVG renderer drops stroked paths, so the glyph must stay a fill. Ships to prod with the next `npm run deploy`.
- 2026-09-17 09:16 UTC Wall search. Convex full text index `search_text` on `messages.text` filtered by status; `messages.search` returns up to 24 live, unhidden matches best first. Search pill in the wall intro: Phosphor `MagnifyingGlass` ghost circle that expands to a pill input with an `X`, 200ms debounce, Escape closes, results replace the grid with "N matches · best first" and Load more hidden. Card markup extracted to `WallCard`. PRD `prds/wall-search.md`. Verified on dev in the browser (light and dark) and via `convex run`. Typecheck and prettier clean.
- 2026-09-17 09:12 UTC `HELD_TERMS` on dev grown to 226 terms (2.1 KB). Added the sexuality cluster (gay, queer, lesbian, homosexual, bisexual, transgender, slurs) and the slavery and race cluster (slave, slavery, plantation, segregation, racial, racism, lynching, kkk, klan, apartheid, master race, uncle tom, jim crow). Only twelve of them pass the safe list today; the rest are stored so the list stays right if the allowlist grows. `gay` was left out of the first pass on purpose because `am i gay` is a live prod ask; it is in now at the owner's call.
- 2026-09-17 09:08 UTC Scroll arrows. New `src/components/ScrollArrows.tsx` mounted at the end of `App.tsx`: fixed bottom right, two 40px ghost circles (36px under 640px) with Phosphor `ArrowUp` and `ArrowDown`, paper fill, ink border, ink fill on hover. Passive scroll and resize listeners; an arrow is hidden (`opacity 0`, `translateY(6px)`, `tabIndex -1`) when its end is within 320px. Styles in `styles.css` after the colophon, plus the reduced motion block. Verified in the browser at top, mid page, and bottom in light and dark. Typecheck and prettier clean.
- 2026-09-17 09:04 UTC Jev can answer asks about itself. Prod had "do you know any other answer other than depends" twice, both `open` at 34% and 41%: the model knew nothing about Jev, so "do you" asks had no common knowledge to draw on. `convex/questions.ts` reply instructions now carry one sentence of self knowledge (answers yes, no, or it depends, one call, no memory, no chat, no browsing) and the `yes` criterion names the case. Dev: the reported ask flips to `yes` (24%), "can you remember what i asked yesterday" gets `no` (68%), "can pigs fly" and "what should i eat tonight" unchanged at 100%. Added `messages.overrideReply` internal mutation for CLI backfills of a misread reply; sets `replyConfidence` to 1. Typecheck and prettier clean. Prod rows `j5731bkb3n22m7m188nvvzeqgn8ej3n8` and `j5798c6x9x69t3kef7kndbm5z18ejkja` still need the backfill after the next prod deploy (see to do).
- 2026-09-17 08:54 UTC `HELD_TERMS` grown to 172 terms (1.7 KB of the 8 KiB env value cap) on dev. Sources: LDNOOBW `en` and rrgeorge `nsfw_list.txt`, filtered to terms whose every word is on the safe list (the rest can never reach the check because `parseMessage` rejects them first), then generic everyday words removed by hand so `am i gay`, `is coffee bad for fat loss`, and `will this kill the battery` still post. The badwordblocker list was dropped: after filtering it still held `a`, `am`, `are`, `the`, `he`, `ok`, `just`, `time`, `job`. Religious names and roles added (jesus, christ, god, pope, priest, minister, bishop, devil, satan, bible, church, crusade, jihad, the major faiths and their followers). `convex/lib/heldTerms.ts` now matches whole words and whole phrases on word boundaries; the letters only substring pass was removed because it read `eat out` inside `great outdoors`. Unit check 17 cases pass. Dev end to end: `is the pope catholic` stored `blocked` with no tokens, `is the great outdoors fun` went to Jev and came back `live`. Typecheck clean. Prod still needs the copy step in to do.
- 2026-09-17 08:48 UTC Guide above the ask box. `src/components/Composer.tsx` renders "Jev answers yes, no, or it depends. Ask something that can be settled that way." plus three safe list example asks as mono text buttons that fill the input. Count note adds "for a yes or no, start with is, can, or will" when a ready ask does not open with a yes or no verb. Driven by prod: 26 of the last 60 live asks were open or statement. PRD `prds/ask-shape.md`. Verified in the browser. Typecheck and prettier clean.
- 2026-09-17 08:35 UTC Prod auth env on `fastidious-oyster-877`. Generated a new RS256 pair (Auth v2 base64 PKCS8, not the v1 `--prod` flag) and set `AUTH_PRIVATE_KEY`, `AUTH_JWKS`, and `ADMIN_USERNAME`. Prod env list now has those three plus `TYPESAFE_API_KEY`.
- 2026-09-17 08:37 UTC Held topics, server only. New `convex/lib/heldTerms.ts` reads a comma separated `HELD_TERMS` env var and matches whole words plus a letters only pass for terms of five or more letters. `messages.send` stores a match as `blocked`, `judged: false`, bumps `submitted` and `blocked`, and skips the judge call. Poster sees the existing "Held back by Jev" line; the wall never sees it; the admin list does. Variable set on the dev deployment. Nothing about the list is in the repo, the client bundle (`rg` on `dist/assets` clean), or the UI. Unit check of the matcher passed 7 cases. Typecheck, build, prettier clean.

- 2026-09-17 08:34 UTC Bar charts on Jev's answers. Each row in `src/components/JevAnswers.tsx` is now question, answer word in ink, then a horizontal band (`.answers__track` / `.answers__fill`) from a shared left baseline with the percent in a fixed 4ch column, so every bar in a card lines up. "Jev says" bars `replyConfidence` for every reply; a held "Fits the wall?" bars the top hazard in accent. Grow in animation, off under reduced motion. Built in CSS on the existing tokens instead of `@mui/x-charts`, which would pull in Material UI and Emotion for a 6px bar. Verified in the browser on wall cards, the Yours list, light and dark. Typecheck and `vite build` clean.
- 2026-09-17 08:28 UTC Bolded "is it a yes, a no, or it depends" in the How it works intro (`src/components/HowItWorks.tsx`). New `.intro .subheading b` rule in `src/styles.css` sets weight 700 and ink so the run stands out of the muted paragraph in both themes. Verified in the browser: weight 700, color ink. Typecheck and prettier clean.
- 2026-09-17 08:26 UTC Hide / Unhide on the wall itself. New `src/hooks/useIsAdmin.ts` (skips the `admin.me` query unless a session is signed in). `Wall.tsx` renders a mono ghost button at the end of each card's meta row for the admin only, calling `admin.setHidden`. Hero nav gains an `Admin` link for the signed in admin. Verified in the browser as admin: 14 cards, 14 buttons, hide then unhide on one card flipped the blur, "hidden by admin" label, and button text; hidden count went 5 to 6 to 5. Typecheck and prettier clean.
- 2026-09-17 07:48 UTC Admin at `/admin`, IP rate limit, clock to one million. PRD `prds/admin-ratelimit-clock.md`. Convex Auth v2 alpha (username + password) wired in `convex/convex.config.ts`, `auth.config.ts`, `auth.ts`; sign up gated to `ADMIN_USERNAME` in `convex/users.ts`; `requireAdmin` in `convex/lib/admin.ts`; `convex/admin.ts` has `me`, paginated `list`, `setHidden`. `messages.hidden` masks text server side in `toPublic`; wall and Yours blur it and read "hidden by admin". `send` checks a 5 per minute fixed window per IP via `ctx.meta.getRequestMetadata()` ahead of the session bucket (now 5 per minute, burst 3). `START_MS` in `convex/lib/counters.ts`, `startedAt` on `stats.counts`, two new rows in `CostTracker` ("On the clock" stopwatch, "At this pace" estimate) plus a start caption. `src/main.tsx` uses `ConvexAuthProvider`, `src/App.tsx` routes `/admin`, `public/robots.txt` disallows it, the page sets `noindex`. Verified: non admin sign up rejected, unauthenticated `setHidden` throws, sixth post in a minute rate limited, hidden card blurs on the wall, stopwatch ticks. Typecheck and prettier clean.

- 2026-09-17 07:39 UTC Judge fact card links to `https://docs.typesafe.ai/introduction`, same `href` prop on `Fact`. Verified in the browser alongside the two Convex cards.
- 2026-09-17 07:37 UTC Convex fact cards link out. `Fact` in `src/components/HowItWorks.tsx` takes an optional `href` and renders the card as an anchor with an ArrowUpRight glyph; Database points to convex.dev, Hosting to the static hosting component page. Hover styles in `src/styles.css` (`.fact--link`). Verified in the browser: both anchors have the right href, `target="_blank"`, `rel="noreferrer"`. Typecheck and prettier clean.
- 2026-09-17 07:11 UTC Section anchors top left. Added a `nav` with "The wall" (`#wall`) and "How it works" (`#how`) at the start of the hero top row, dotted divider before the Powered by credit, `scroll-margin-top` on both sections. Files: `src/App.tsx`, `src/styles.css`. Verified click scrolls to `#how` at 1280, no horizontal overflow at 375, divider hidden at narrow widths, dark mode uses the hairline token. Typecheck and prettier clean.
- 2026-09-17 07:03 UTC Colophon at the bottom. Moved "Demo app built by waynesutton.ai" from the hero top row to a centered two line block at the end of `main`, added "Demo app not associated with TypeSafe AI" under it. Files: `src/App.tsx`, `src/styles.css`. Verified both lines center at 640 on a 1280 viewport and sit at the document bottom. Typecheck clean.
- 2026-09-17 07:00 UTC Caldera light mode, no header, no footer. PRD `prds/caldera-light.md`. Light is pumice, limestone, obsidian, one ember, sulfur tag, Archivo compressed display, DM Sans 500, DM Mono labels, flat, dotted dividers. Dark keeps its palette and Inter with the wash removed. Nav and Footer deleted; credits and a mono theme toggle live in a caption row at the top of the hero, with crop marks. Three column hero: copy, composer, ember count panel with cost rows. Verified at 1280 by 800 (headline, input, odometer, four cost rows above the fold, both themes) and 375 (no overflow). `og.png` regenerated. Typecheck clean.

- 2026-09-17 06:55 UTC Convex verified `www.askjev.ai`; set production `CONVEX_SITE_URL` to `https://www.askjev.ai`. Domain setup complete without an app deployment.

- 2026-09-17 06:52 UTC Configured `www.askjev.ai` for production Convex HTTP Actions and Cloudflare apex-to-www 301 with path/query preservation and Always Use HTTPS. Public DNS and live HTTPS verified; browser shows Jev online and existing wall data. No app deployment. New design/share card remain pending deployment. Typecheck passed. See `prds/setup-domain-task.md` for exact records and verification status.

- 2026-09-17 06:28 UTC Moved the odometer and cost cards into the hero as a right column beside the composer, above the fold at 1280 by 800. Stacks under 1000px. PRD `prds/stats-above-fold.md`. Typecheck clean, verified at 1280 and 375.
- 2026-09-17 06:22 UTC Removed the dark top bar; pill nav is the only header. Footer credit now "Demo app built by waynesutton.ai". Typecheck clean, verified in browser.

- 2026-09-17 06:18 UTC Copy made honest about what Jev does (judges, never replies) in hero, How it works, meta, and the share card. Card regenerated. Typecheck clean.

- 2026-09-17 06:12 UTC Share card and social meta. `public/og.png` from `scripts/og-card.html`, Open Graph and X `summary_large_image` tags in `index.html` with absolute prod URLs. Build confirmed `dist/og.png` and six tag matches.

- 2026-09-17 06:08 UTC Daylight redesign. Light is the default, dark stays behind a sun and moon toggle (`useTheme`, `data-theme`, stored, applied before paint). Frosted accent wash hero with the composer card in the middle; Yours trimmed to three with answers collapsed. Counter in its own section, cost tracker as four accent bar stat cards, wall cards with mood colored bars, periwinkle Jev chip, dark top bar plus sticky pill nav, Phosphor icons. PRD `prds/light-redesign.md`. Verified in browser at 1280, 720 and 375: caret blinks in the centered input on load, toggle flips and persists, chips render as pills, no horizontal overflow. Typecheck clean.

- 2026-09-17 05:52 UTC Jev answers the ask. Sixth Choice question `reply` (yes, no, depends, open, statement) in `convex/questions.ts`, plumbed through `judge.ts`, `schema.ts`, `messages.ts`. "Jev says" row first in the toggle, cream `Jev: yes|no|depends` chip first on wall cards. PRD `prds/jev-says.md`. Verified live: "can pigs fly" no 100% (304ms), "will the sun rise tomorrow" yes 97% (181ms), "what should I cook tonight" not a yes or no question (155ms). Old rows render without the row. Typecheck clean.

- 2026-09-17 05:43 UTC Jev's answers toggle rewritten in plain language (`src/components/JevAnswers.tsx`): "How does it feel?", "What is it about?", "Fits the wall?". Safety probes folded into the last row with a worded reason when held. Verified in browser: "my dog learned a trick" shows happy 68%, nature 65%, yes 99% sure. Typecheck clean.
- 2026-09-17 05:40 UTC Renamed to Ask Jev, up to a million times. Word range 3 to 15 (`MIN_WORDS`, `MAX_WORDS`, `parseMessage`). Placeholder "ask jev anything". How it works reduced to the requested copy plus five facts, no mention of the word gates. Status strings say Jev online or offline. Long wall cards render at 18px. PRD `prds/ask-jev.md`. Verified in browser: `is coffee good` (3 words) live, FLAT food 312ms; a 15 word ask live, HAPPY nature 284ms; 16 words shows "16 words. Cut 1" with Post disabled; typecheck clean.
- 2026-09-17 05:20 UTC Renamed to A Million Jevs and added the profanity blocklist (gate 1, `obscenity`) with `***` masking and a disabled Post. Added jev, jevs, convex, typesafe, demo to the allowlist. Removed kids and classroom wording everywhere. PRD `prds/a-million-jevs.md`. Verified: typecheck clean, 11 parser cases pass, `sh1t` masked in the browser, `a million jevs are here` posted and judged CALM in 289ms.
- 2026-09-17 05:10 UTC Linked to cloud project `jev-5-words-playground`. `TYPESAFE_API_KEY` set on dev and prod. First judged message: HAPPY, nature, 267ms, $0.000031.
- 2026-09-17 05:10 UTC Added "Show Jev's answers" toggle to wall cards and your own posts (`src/components/JevAnswers.tsx`). Removed the vertical edge label. Placeholder now "type my five words here".

- 2026-09-17 04:05 UTC Wrote PRD `prds/five-to-million.md`.
- 2026-09-17 04:08 UTC Scaffolded Vite + React + TypeScript app, installed Convex and three official components.
- 2026-09-17 04:08 UTC Generated `convex/lib/safeWords.ts` (12,530 tokens) with `scripts/buildSafeWords.mjs`.
- 2026-09-17 04:09 UTC Backend: schema, questions, typed TypeSafe client, send, retry, wall, mine, judge, stats. First `npx convex dev --once` deployed clean with anonymous local deployment.
- 2026-09-17 04:12 UTC Frontend: hero counter with odometer digits, composer with live word chips, yours strip, wall grid, how it works, footer. Typecheck and `vite build` pass.
- 2026-09-17 04:14 UTC Verified via `npx convex run` and browser: post goes judging to live, counter increments, six words and unsafe words throw, fourth burst post is rate limited.
- 2026-09-17 04:16 UTC Polish: nav fade over content, unsafe word count in note, facts grid fits four across.
- 2026-09-17 04:20 UTC Realtime cost tracker: tokens per message, counters, `stats.cost`, hero strip, per card cost and latency. Typecheck clean, query returns zeros until a key is set.
- 2026-09-17 04:50 UTC Composer autofocus on load, "start typing here" label, focus driven underline, CSS blink caret while empty, edge label hidden under 1200px. Verified in browser: textbox focused on load, solid underline, caret visible.
