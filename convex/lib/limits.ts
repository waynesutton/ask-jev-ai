// Plain numbers shared by the server and the client. No imports, so the
// browser bundle can read them without dragging the rate limiter along.
export const ANON_POSTS_PER_MINUTE = 5;
export const USER_POSTS_PER_MINUTE = 20;
export const ANSWERS_PER_DAY = 200;
